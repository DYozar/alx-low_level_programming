#include "main.h"
/**
 * main - Checks the _strcpy function.
 *
 * Return: Always 0.
 */
int main(void)
{
	int i;

/* Iterate over source string and copy each character to destination */
	if (dest == NULL)
		return (NULL);
	for (i = 0; src[i] != '\0'; i++)
	    dest[i] = src[i];
	
	return (dest);
}

